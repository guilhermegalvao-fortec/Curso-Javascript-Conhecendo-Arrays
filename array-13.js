// Escreva um código que utilize o loop for/of para
// iterar e imprimir cada elemento de um array.
let names = ['guilherme', 'valdery', 'fernando'];

for (let index = 0; index < names.length; index++) {
    console.log(names[index]);
}