// Crie uma função que receba uma array e 
// imprima no console o número do índice e o elemento.
let numbers = [7, 13, 17, 20, 24];

for (let index = 0; index < numbers.length; index++) {
    console.log(index, numbers[index]);
}