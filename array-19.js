// Utilize o método forEach para imprimir cada
// elemento de um array juntamente com seu índice.

let arrayNumbers = ['guilherme', 'mauro', 'bione', 'gabriel', 'mota'];

arrayNumbers.forEach( (string, i) => {
    console.log(i, string);
});