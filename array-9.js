const menuPrincipal = ['macarrão', 'feijão'];
const menuDeSobremesa = ['pudim', 'sorvete'];

const menuCompleto = menuPrincipal.concat(menuDeSobremesa);
console.log(`Veja o menu completo: ${menuCompleto}`);